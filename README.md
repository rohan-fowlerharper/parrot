TODO: add README

Basic Usage:

```sh
# install globally
pnpm add -g https://github.com/enspiral-dev-academy/parrot.git

# init
parrot init [token]
# or
parrot init
code ~/.parrot/.env # paste github access token here

# run
parrot <url> [branch] # branch is optional if you provide a url with a branch in it
```